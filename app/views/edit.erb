<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Post</title>
  </head>
  <body>
    <h1>Edit Post</h1>
    <form action="/posts/<%= @post.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <p><label>Post Title: </label><input type="text" id="name" name="post[name]" value="<%= @post.name %>" /></p>
      <p><label>Type Your Post Here: </label><input type="text" id="content" name="post[content]" value="<%= @post.content %>" /></p>
      <p><input type="submit" value="submit" /></p>
    </form>
  </body>
</html>
